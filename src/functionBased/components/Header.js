import React from "react";
import '../../style.css';

const Header =()=>{
    return (
        <header>
            <h1 className="header-heading">Todos</h1>
        </header>
    );
}

export default Header;